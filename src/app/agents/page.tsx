import { Placeholder } from "@/components/placeholder";

export default function AgentsPage() {
  return (
    <Placeholder
      title="Agents"
      description="Chat where the model runs in a tool-using loop, calling built-in and MCP tools to get things done."
      phase="Coming in Phase 4"
    />
  );
}
